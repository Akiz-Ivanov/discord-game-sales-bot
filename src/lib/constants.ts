export const WISHLIST_LIMIT = 100

export const wishlistLimitReachedWithRemoveMessage = () =>
  `Your wishlist is at the ${WISHLIST_LIMIT}-game limit — pick something to remove:`

export const AUTOCOMPLETE_MIN_QUERY_LENGTH = 3
